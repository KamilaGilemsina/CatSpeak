<Application x:Class="CatSpeak.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:CatSpeak"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <DropShadowEffect x:Key="ShadowEffect" BlurRadius="8" ShadowDepth="2" Opacity="0.3" Color="Black"/>
        <Style TargetType="Window">
            <Setter Property="FontFamily" Value="/Fonts/#Kalam"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Background" Value="#F5FFFA"/>
        </Style>
        <Style TargetType="Page">
            <Setter Property="FontFamily" Value="/Fonts/#Kalam"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Background" Value="#F5FFFA"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#20B2AA"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="/Fonts/#Kalam"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="/Fonts/#Kalam"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Foreground" Value="#2F4F4F"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="FontFamily" Value="/Fonts/#Kalam"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Background" Value="#E0FFFF"/>
            <Setter Property="BorderBrush" Value="#20B2AA"/>
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="FontFamily" Value="/Fonts/#Kalam"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Background" Value="#E0FFFF"/>
            <Setter Property="BorderBrush" Value="#20B2AA"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="FontFamily" Value="/Fonts/#Kalam"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Background" Value="#E0FFFF"/>
        </Style>
    </Application.Resources>
</Application>
